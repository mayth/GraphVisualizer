解析の流れ
　Tokenizerクラスで与えられた文字列をトークン単位に分割する。
　トークン単位に分割した後、NodeのParseメソッドに渡す。
　Parseメソッドは葉ノード（式ではなく項）になるまで再帰的に呼び出される。

精度
　15桁くらい。double型の仕様。
　decimal型を使用しても、Mathクラスにある静的メソッドはそのほとんどがdoubleで値を扱うから意味がない。Math.Absはdecimal型のオーバーロードあり。

